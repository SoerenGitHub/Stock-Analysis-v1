import { ReactiveControlDirective } from './reactive-control.directive';

describe('ReactiveControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactiveControlDirective();
    expect(directive).toBeTruthy();
  });
});
